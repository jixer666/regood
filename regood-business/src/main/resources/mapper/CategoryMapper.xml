<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.business.mapper.CategoryMapper">

    <resultMap type="com.abc.business.domain.entity.Category" id="CategoryResult">
        <result property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="icon" column="icon"/>
        <result property="parentId" column="parent_id"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCategoryVo">
        select category_id, name, icon, parent_id, sort, status, create_time, update_time
        from tb_category
    </sql>

    <select id="selectCategoryList" resultMap="CategoryResult">
        <include refid="selectCategoryVo"/>
        <where>
            <if test="status != null">and status = #{status}</if>
        </where>
        order by sort
    </select>

</mapper>
