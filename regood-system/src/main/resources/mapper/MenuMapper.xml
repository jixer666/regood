<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.system.mapper.MenuMapper">

    <resultMap type="Menu" id="MenuResult">
        <result property="menuId"    column="menu_id"    />
        <result property="parentId"    column="parent_id"    />
        <result property="menuName"    column="menu_name"    />
        <result property="path"    column="path"    />
        <result property="component"    column="component"    />
        <result property="menuType"    column="menu_type"    />
        <result property="orderNum"    column="order_num"    />
        <result property="perms"    column="perms"    />
        <result property="icon"    column="icon"    />
        <result property="hidden"    column="hidden"   />
        <result property="userId"    column="user_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
        <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectMenuVo">
        select menu_id, parent_id, menu_name, path, component, menu_type, order_num, perms, icon, hidden, user_id, create_time, update_time, status, ver from tb_menu
    </sql>
    <select id="selectMenusByUserId" resultMap="MenuResult">
        select m.* from tb_menu m
        left join tb_role_menu rm on rm.menu_id = m.menu_id
        left join tb_role r on r.role_id = rm.role_id
        left join tb_user_role ur on ur.role_id = r.role_id
        <where>
            <if test="userId != null">ur.user_id = #{userId}</if>
        </where>
    </select>
    <select id="selectMenuIdsByRoleId" resultType="java.lang.Long">
        select m.* from tb_menu m
        left join tb_role_menu rm on rm.menu_id = m.menu_id
        left join tb_role r on r.role_id = rm.role_id
        <where>
            <if test="roleId != null">r.role_id = #{roleId}</if>
        </where>
    </select>
    <select id="selectMenusByParentId" resultMap="MenuResult">
        <include refid="selectMenuVo"/>
        where parent_id = #{menuId}
    </select>
    <select id="selectMenusByMenuType" resultMap="MenuResult">
        <include refid="selectMenuVo"/>
        where menu_type = #{menuType}
    </select>
    <select id="selectMenusByMenuDTO"  resultMap="MenuResult">
        select distinct m.* from tb_menu m
        left join tb_role_menu rm on rm.menu_id = m.menu_id
        left join tb_role r on r.role_id = rm.role_id
        left join tb_user_role ur on ur.role_id = r.role_id
        <where>
            <if test="menuName != null">m.menu_name like concat('%', #{menuName}, '%')</if>
            <if test="front != null">
                <if test="front">m.menu_type = 4</if>
                <if test="!front">m.menu_type != 4</if>
            </if>
        </where>
    </select>

</mapper>
