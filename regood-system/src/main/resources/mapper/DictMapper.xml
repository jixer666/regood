<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.system.mapper.DictMapper">

    <resultMap type="Dict" id="DictResult">
        <result property="dictId"    column="dict_id"    />
        <result property="dictName"    column="dict_name"    />
        <result property="dictKey"    column="dict_key"    />
        <result property="userId"    column="user_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
        <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectDictVo">
        select dict_id, dict_name, dict_key, user_id, create_time, update_time, status, ver from tb_dict
    </sql>
    <select id="selectDictList" resultMap="DictResult">
        <include refid="selectDictVo" />
        <where>
            <if test="dictName != null and dictName != ''">and dict_name like concat('%', #{dictName}, '%')</if>
            <if test="dictKey != null and dictKey != ''">and dict_key like concat('%', #{dictKey}, '%')</if>
        </where>
    </select>


</mapper>
