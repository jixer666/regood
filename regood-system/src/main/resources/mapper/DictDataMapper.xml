<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.system.mapper.DictDataMapper">

    <resultMap type="DictData" id="DictDataResult">
        <result property="dictDataId"    column="dict_data_id"    />
        <result property="dictId"    column="dict_id"    />
        <result property="dictLabel"    column="dict_label"    />
        <result property="dictValue"    column="dict_value"    />
        <result property="userId"    column="user_id"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
        <result property="status"    column="status"    />
        <result property="ver"    column="ver"    />
    </resultMap>

    <sql id="selectDictDataVo">
        select dict_data_id, dict_id, dict_label, dict_value, user_id, create_time, update_time, status, ver from tb_dict_data
    </sql>
    <select id="selectDictDataList" resultMap="DictDataResult">
        <include refid="selectDictDataVo" />
        <where>
            <if test="dictLabel != null and dictLabel != ''">and dict_label like concat('%', #{dictLabel}, '%')</if>
            <if test="dictValue != null and dictValue != ''">and dict_value like concat('%', #{dictValue}, '%')</if>
            <if test="dictId != null">and dict_id = #{dictId}</if>
        </where>
    </select>
    <select id="selectDictDataByDictKey" resultMap="DictDataResult">
        select dd.* from tb_dict_data dd
        left join tb_dict d on d.dict_id = dd.dict_id
        where d.dict_key = #{dictKey}
    </select>


</mapper>
